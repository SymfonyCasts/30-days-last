<form method="post" action="{{ path('app_planet_delete', {'id': planet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planet.id) }}">
    <button class="btn">Delete</button>
</form>
