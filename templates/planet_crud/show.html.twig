{% extends 'base.html.twig' %}

{% block title %}Planet{% endblock %}

{% block body %}
    <h1>Planet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ planet.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ planet.description }}</td>
            </tr>
            <tr>
                <th>LightYearsFromEarth</th>
                <td>{{ planet.lightYearsFromEarth }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_planet_crud_index') }}">back to list</a>

    <a href="{{ path('app_planet_crud_edit', {'id': planet.id}) }}">edit</a>

    {{ include('planet_crud/_delete_form.html.twig') }}
{% endblock %}
